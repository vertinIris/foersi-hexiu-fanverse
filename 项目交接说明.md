# 佛尔思和休同人作品站 — 项目交接说明

> 自包含文档，可直接粘贴到新对话中作为上下文。无需额外追问即可理解项目全貌。
> 最后更新：2026-09-07，代码运行于 `http://127.0.0.1:3001`。

---

## 1. 项目背景与目标

**主题**：「佛尔思和休（Foxiu & Hexiu）」同人作品站，服务于同人创作的**在线阅读**与**作家上传/管理**。

**初始需求**（用户提出）：作品分类与检索、章节式阅读界面、作家注册与作品管理后台、上传与编辑功能。

**我们已补充的遗漏项**：用户评论、收藏系统、Tagging 标签云。

**最新定位调整（2026-09）**：从「站长观点展示台」转变为**「收藏与展示平台」**——观点性内容（角色志、关于本站）改为**创作者可编辑的数据**，并补齐图片、视频等多媒体形式。原因：用户明确本平台是收藏展示而非个人观点站，主观文案应交由创作者维护。

**演示账号**：`休之笔` / `demo1234`（种子中标记为 `role: admin`）。

---

## 2. 技术栈与关键依赖

| 层 | 选型 | 理由 |
|---|---|---|
| 运行时 | **Node.js ≥ 18 内置 `http`** | 零依赖，消除 npm install/编译风险，保证可运行 |
| 存储 | **JSON 文件 `data/db.json`** | 零依赖；规模 < 2000 作品 / < 50MB 足够 |
| 前端 | **原生 HTML/CSS/JS**，无框架、无 CDN、无构建 | 零依赖、可直接静态托管 |
| 密码哈希 | `crypto.scryptSync` + 随机盐 | Node 内置，抗暴力破解 |
| 会话 | **服务端 Token**（`db.sessions` 映射） | 支持即时吊销（登出/封号），不用 JWT |
| 生产运维 | Nginx + systemd + logrotate（仅配置，未部署） | 沿用约定，不引入 PM2/Docker |

**关键约束**：不引入任何第三方依赖（无 `npm install`），不擅自换技术栈（如 SQLite/Redis/Express）。何时需要换：作品 > 5000 或 `db.json` > 50MB 时触发评估。

**端口**：默认 `PORT || 3000`；当前人为指定 `PORT=3001`（绕端口冲突）。`HOST` 默认 `127.0.0.1`（外部不可直连，限流依赖此前提）。

---

## 3. 目录结构及核心文件作用

```
C:\foersi-hexiu-fanverse\
├── server.js              # 核心 HTTP 服务：路由 /api/* + 静态托管，~2400 行总代码
├── store.js               # JSON 存储层：getDB/save/genId + 种子数据 + 迁移 + cleanup
├── lib/
│   ├── auth.js            # hashPassword / verifyPassword / genToken
│   ├── logger.js          # 按天切分日志，Token 脱敏为前 8 位
│   └── ratelimit.js       # 内存限流（分级规则）
├── public/
│   ├── index.html / browse.html / work.html / read.html
│   ├── auth.html / dashboard.html
│   ├── characters.html / about.html     # 已改为动态加载（原硬编码观点）
│   ├── author.html / notifications.html / legal.html
│   ├── css/style.css       # 响应式 + 明/暗主题
│   ├── js/common.js        # 公共库：API 客户端/导航/主题/escapeHtml/renderContent/workCard
│   └── js/{index,browse,work,read,auth,dashboard,author,notifications,pages}.js
├── data/db.json           # 运行数据（seed 在 store.js）
├── backups/               # 数据库备份
├── logs/                  # 运行日志（access-YYYY-MM-DD.log）
├── test/                  # node --test 套件（unit + api）
└── *.md                   # 方案.md / 生产上线方案.md / 功能完善总结.md / 设计分析报告.md
```

**模块交互**：前端 `common.js` 的 `API` 客户端 `fetch('/api'+path)` 带 `Authorization: Bearer <token>` → `server.js` 的 `handleApi` 解析路由 → `store.js` 读写 `db.json` → `lib/auth.js` 校验密码/Token。

---

## 4. 已完成的功能与实现要点

**核心（初版）**
- 作品 CRUD + 章节 CRUD；列表支持 `q/category/tag/status/sort/type/rating/mature/page/pageSize` 筛选
- 章节式阅读：目录/上下章/字号/主题记忆（`localStorage`）
- 评论（需登录）、收藏 toggle（实时计数）、标签云 + 标签筛选
- 作家后台：作品/章节管理；作者主页 `author.html`；关注作者；通知中心 `notifications.html`
- 内容分级 G/T/M + 年龄门控（M 级默认隐藏，需年龄确认）
- 非官方声明页 `legal.html`、角色志 `characters.html`、关于页 `about.html`
- 响应式（导航折叠、网格自适应、后台表格转卡片）；明/暗主题

**生产加固（2026-09，server.js/store.js）**
- 原子写入：`save()` 改为「写 `.tmp` + `rename`」，坏 JSON 改名保留而非静默回退种子
- 消除写放大：views 计数改内存缓冲 + 60s 批量落盘（原每阅读一次全量重写库）
- `/api/health` 健康检查端点
- `lib/logger.js` 日志 + Token 脱敏；`lib/ratelimit.js` 限流（10/min 登录、5/10min 注册等）+ `Retry-After`
- 请求体上限 1MB（上传路由 6MB），超限返回 413；错误响应按 `NODE_ENV` 脱敏
- Token 7 天滑动过期 + 启动清理僵尸会话；进程异常兜底 + 优雅退出
- 测试套件：单元测试 8/8、端到端 10/10（`node --test test/`）

**最新一轮（收藏展示化，2026-09，部分落地）**
- `store.js` 新增 `sitePages` 集合（key/title/subtitle/content/updatedAt/updatedBy）+ `media` 集合；种子用户 `u_demo` 置 `role: admin`
- `server.js` 新增接口（见下）；作品表加 `cover` 字段（封面 URL）
- `common.js` 新增 `renderContent()`：转义后解析图片 `![alt](url)` 与视频 `@video(url)`（B站/YouTube→iframe）
- `characters.html`/`about.html` 改为动态加载 `sitePages`；`pages.js` 渲染；首页「编辑精选」改为中性「佛休·热门作品」
- 作品卡 `workCard` 支持封面图（`safeUrl` 校验）
- 阅读页正文用 `renderContent` 渲染图片/视频
- 后台新增「站点内容」页签（仅 admin 可见）：编辑角色志/关于页，支持插入图片/视频
- `server.js` 图片上传 `/api/upload`：base64 → `public/uploads/YYYY/MM/`，限 5MB，校验 MIME + 魔数，SVG 清洗脚本

**关键接口签名（节选）**
```js
// 站点页面（角色志/关于）：创作者可编辑
GET  /api/pages/:key            -> { page: {key,title,subtitle,content,updatedAt,updatedBy} }
PUT  /api/pages/:key   (admin)  -> body:{title?,subtitle?,content?}

// 图片上传（独立放宽 body 上限）
POST /api/upload       (登录)   -> body:{data:"data:image/png;base64,...",name?} -> {url,size,mime}

// 作品封面
POST/PUT /api/works[/:id]       body.cover: string (URL，≤500 字符)
```

**配置文件示例（环境变量）**
```bash
PORT=3001 HOST=127.0.0.1 NODE_ENV=production node server.js
```

---

## 5. 当前进度与未完成事项

**已完成**：核心功能、生产加固 10 项、测试套件、站点页面数据库化与多媒体渲染器、上传接口、后台编辑页签、首页去观点化。

**未完成 / 半成品**（见第 7 节风险）：
1. 作品**详情页**封面仍用渐变占位（`work.js` L27），未渲染 `work.cover` URL（卡片已支持）
2. 现有 `db.json` 早于 admin 种子生成，所有用户 `role=author` → 后台「站点内容」页签不显示、页面无法编辑（需迁移或重置 db）
3. 部署未做（仅本地运行）；非 git 仓库，无版本记录

---

## 6. 已有的代码规范或约定

- **零依赖**：只用 Node 内置模块，禁止 `npm install` 第三方包
- **源码为权威**：文档（方案.md 等）同步必须以 `server.js`/`store.js` 实际路由与字段为准，不得凭二手文档
- **XSS 防护**：所有用户输入渲染经 `escapeHtml` 转义；多媒体用 `renderContent`（先转义再解析 `![]()`/`@video()`）；URL 用 `safeUrl` 校验
- **权限二次校验**：写操作服务端校验 `work.authorId === user.id`（返回 401/403）
- **数据安全**：`save()` 原子写；views 缓冲落盘；Token 过期管理
- **端口/监听**：默认 3000；限流依赖 `X-Forwarded-For`，故 `HOST` 必须保持 `127.0.0.1`（上线经 Nginx 反向代理）
- **用户偏好**：改动完成后按惯例应 `git commit + push`（本机用 D 盘系统 git + wincred 助手），但当前无仓库
- **演示账号提示**：登录页标注 `休之笔 / demo1234`

---

## 7. 已知问题与待解决风险

| # | 问题 | 影响 | 建议修复 |
|---|---|---|---|
| R1 | `db.json` 用户 `role` 均为 `author`（早于 admin 种子） | 站点内容编辑器不可达，页面无法编辑 | `store.js` ensureLoaded 加迁移：`db.users.forEach(u=>{if(u.id==='u_demo')u.role=u.role||'admin'})`；或 `rm data/db.json` 重置 |
| R2 | 详情页封面未用 `work.cover` | 详情页无图，仅卡片有 | `work.js` L27 改为 `work.cover ? <img class="work-cover" src=safeUrl(work.cover)> : 占位` |
| R3 | JSON 单文件存储 | 高并发写入有风险；坏文件虽已保护但仍是单点 | 规模超阈值时评估 SQLite（不擅自换） |
| R4 | 无邮件验证/注册审核 | 任意人可注册发文 | 按需补 admin 审核流 |
| R5 | 非 git 仓库 | 所有改动（含 10 项加固 + 新文件）无回滚点 | 立即 `git init` + 首次提交（见第 8 节） |
| R6 | 部署区域风险 | EdgeOne 默认「全球不含大陆」，大陆访问 401 | 需 ICP 备案或换通道；本地演示够用则暂缓 |
| R7 | 上传仅 base64 内联 | 大图请求体重；无对象存储 | 当前 5MB 内可接受 |

---

## 8. 下一步待办清单

**优先级 P0（数据正确性，立即做）**
1. 修复 R1：在 `store.js` 加 admin 角色迁移（或重置 `db.json`），使「站点内容」编辑器可用
2. 修复 R2：详情页渲染 `work.cover` 封面图
3. `git init` + 首次提交（R5）—— 固化当前成果

**P1（体验补全）**
4. 后台作品编辑支持封面：上传或填 URL，写入 `work.cover`
5. 验证多媒体端到端：上传图 → 在角色志/章节正文插入 → 渲染
6. 同步 `README.md`/`方案.md`：补 `sitePages`、上传接口、admin 角色说明

**P2（生产部署，需 Linux 服务器）**
7. 按 `生产上线方案.md` §5 落地 Nginx + systemd + logrotate + 备份 cron
8. 部署决策：EdgeOne（需备案）或本地/云服务器

**验证命令**
```bash
cd C:\foersi-hexiu-fanverse
PORT=3001 node server.js                    # 启动
curl http://127.0.0.1:3001/api/health      # 探活
node --test test/                          # 跑测试
node --check server.js store.js public/js/*.js   # 语法
```

---

## 附：关键决策速查

- **不用 JWT**：会话需即时吊销 → 服务端 Token
- **不换数据库**：规模未达阈值，JSON 够用且零依赖
- **观点内容数据库化**：平台定位是收藏展示，主观文案交创作者
- **限流靠 X-Forwarded-For**：安全前提是 `HOST=127.0.0.1`，上线必须经反向代理
- **文档以源码为准**：曾因凭文档同步导致 3 处接口字段错误，已记入项目记忆
