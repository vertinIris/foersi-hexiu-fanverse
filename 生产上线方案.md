# 佛尔思 · 同人创作平台 — 生产上线技术方案

> 目标：在**不推翻既有技术决策**的前提下，把当前已可运行的站点加固到「可直接正式上线」的状态。
> 本文所有接口字段、数据模型均按 `server.js` / `store.js` / `lib/auth.js` 实际代码核对，非推测。

---

## 0. 方案定位与假设条件

### 0.1 与既有文档的关系

| 文档 | 定位 | 状态 |
|---|---|---|
| `方案.md` | 初版设计（需求复盘、选型理由） | 已被后续迭代超越，保留作设计溯源 |
| `功能完善总结.md` | 佛尔思中心向重构后的功能现状 | 现行 |
| `设计分析报告.md` | 设计诊断与 P0/P1/P2 待办 | 现行 |
| **本文档** | **生产上线的工程实施方案** | **新增** |

### 0.2 假设条件（Assumptions）

如与实际不符，请指出，相应章节需调整。

| 编号 | 假设 | 不成立时的影响 |
|---|---|---|
| **A1** | 规模：注册用户 < 5,000，作品 < 2,000，章节 < 20,000，日 PV < 50,000 | 超出则需换数据库（见 §8） |
| **A2** | 单机单进程部署，不做集群与横向扩展 | 需集群则 JSON 存储必须替换 |
| **A3** | 已有域名，且**完成 ICP 备案**（面向中国大陆用户） | 未备案只能用境外节点，大陆访问不稳定 |
| **A4** | 无支付、无邮箱验证、无短信 | 涉及则需引入外部服务，不在本方案范围 |
| **A5** | 内容为《诡秘之主》二创，已通过 `legal.html` 声明非官方与 transformative use | 合规风险由内容分级 + 声明承担 |
| **A6** | 封面沿用按 ID 哈希的渐变占位，**不引入对象存储** | 要真实封面需另立方案 |
| **A7** | 备份落在**本机磁盘**（非异地） | 需异地则加 rsync/对象存储 |
| **A8** | 服务器为 Linux（Ubuntu 22.04 LTS），Node ≥ 18 | Windows 部署见 §5.6 |

### 0.3 当前代码的上线就绪度（实测结论）

已核对源码后，当前状态分为三类：

| 类别 | 项目 | 结论 |
|---|---|---|
| ✅ **已就绪** | scrypt+盐密码哈希、`timingSafeEqual` 比对、Bearer 鉴权、写操作服务端二次校验作者归属、`escapeHtml` 转义、静态目录穿越防护、分级与年龄门控、响应式 | 可直接上线 |
| ⚠️ **必须加固** | ① `save()` 非原子写 ② 浏览一次作品即全量重写数据库 ③ Token 永不过期 ④ 无健康检查端点 ⑤ 无访问/错误日志 ⑥ 无速率限制 ⑦ 无请求体大小限制 ⑧ 错误响应泄露内部信息 | **见 §7，上线前必做** |
| 🔶 **需协商** | JSON 单文件存储换 SQLite、Token 换 JWT、CDN 与封面上传、评论嵌套与审核 | 见 §8，本方案**不擅自引入** |

---

## 1. 架构与技术选型

### 1.1 选型决策表

**原则：沿用既有决策；确需新增的，只加运维层组件与 Node 内置能力，不动技术栈。**

| 层 | 选型 | 状态 | 推荐理由 |
|---|---|---|---|
| 运行时 | **Node.js ≥ 18**（LTS） | 沿用 | 已有代码零依赖，内置 `node:test` 可直接做单元测试，无需引入测试框架 |
| Web 层 | **Node 内置 `http`** | 沿用 | 已稳定运行；Express 仅省路由代码，却引入依赖与升级面，收益不抵风险 |
| 存储 | **JSON 单文件**（`data/db.json`） | 沿用（A1 规模内） | A1 规模下全量读写仍在毫秒级；换数据库是**架构级变更**，须你确认后单独立项 |
| 前端 | **原生 HTML/CSS/JS** | 沿用 | 无构建步骤 = 无构建失败风险，部署即拷贝 |
| 密码哈希 | **`crypto.scryptSync` + 16B 随机盐** | 沿用 | Node 内置，抗 GPU 暴力破解优于 bcrypt 之外的常规选择 |
| 会话 | **随机 48 字符 Token + 服务端映射** | 沿用 + 补过期 | 服务端可主动吊销，比无状态 JWT 更适合本站点；JWT 一旦签发无法即时失效 |
| 反向代理 / TLS | **Nginx + Let's Encrypt** | **新增** | 生产不可裸奔 HTTP；Node 直接处理 TLS 会增加进程负担与证书运维复杂度 |
| 进程守护 | **systemd** | **新增** | Linux 自带，零额外依赖；PM2 需 npm 全局安装，违背零依赖初衷 |
| 日志 | **Node 内置 `fs` 写文件 + logrotate** | **新增** | 不引第三方日志库 |
| 备份 | **cron + `cp` 脚本** | **新增** | 最简可靠，无外部依赖 |

> **明确不引入**：Express、SQLite/Postgres、Redis、JWT、PM2、Docker、Webpack/Vite、对象存储、第三方日志/监控 SDK。
> 如你希望其中任何一项，请单独提出，我会另出变更方案。

### 1.2 模块职责划分

```
foersi-hexiu-fanverse/
├── server.js         HTTP 入口：路由分发、API 处理、静态资源、全局错误兜底
├── store.js          数据访问层：加载/原子保存/ID 生成/字数统计（唯一写入口）
├── lib/auth.js       鉴权：密码哈希与校验、令牌生成
├── lib/logger.js     日志：分级写入 + 按天切分        ← 新增（§7.4）
├── lib/ratelimit.js  速率限制：内存滑动窗口          ← 新增（§7.5）
├── data/db.json      运行时数据
├── logs/             访问日志与错误日志              ← 新增
├── backups/          数据库定时备份                  ← 新增
└── public/           静态前端（11 个页面 + css + js）
```

**职责边界（重要）**

- `store.js` 是**唯一**能写 `data/db.json` 的模块，`server.js` 不得直接操作文件 —— 保证原子写与备份策略可集中维护。
- `server.js` 只做：解析请求 → 业务校验 → 调 `store` → 构造响应。
- 前端 `public/js/common.js` 统一封装 API 客户端、主题、导航、toast；各页脚本只处理本页渲染。

### 1.3 模块交互关系

```
浏览器 ──HTTPS──> Nginx(:443)
                    │ ① TLS 终止
                    │ ② 静态资源直出（带缓存头）
                    │ ③ /api/* 反向代理到 127.0.0.1:3000
                    ▼
              Node http(:3000, 仅监听 127.0.0.1)
                    ├─ getUser()      → lib/auth.js 校验 Bearer
                    ├─ handleApi()    → 业务校验（归属/分级/参数）
                    ├─ logger         → logs/access-YYYY-MM-DD.log
                    ├─ ratelimit      → 内存窗口（登录/注册/评论）
                    └─ store.save()   → 原子写 data/db.json
```

**关键约束**：Node 只监听 `127.0.0.1`，**不对外暴露 3000 端口**，所有外部流量必须经 Nginx。

### 1.4 请求处理链路

1. Nginx 终止 TLS，转发 `X-Real-IP` / `X-Forwarded-For` / `X-Forwarded-Proto`
2. `server.js` 判断路径前缀：`/api/` 走 API，否则静态
3. API 分支：`getUser(req)` 解析 Bearer → `readBody`（**带大小上限**）→ 路由匹配 → 业务校验 → `store.save()` → JSON 响应
4. 全程 `try/catch` 兜底，异常写错误日志并返回**脱敏**的 500

---

## 2. 功能清单与实现细节

### 2.1 功能总表

| 编号 | 功能 | 状态 | 入口 |
|---|---|---|---|
| F1 | 注册 / 登录 / 登出 / 当前用户 | ✅ | `/api/register` `/api/login` `/api/logout` `/api/me` |
| F2 | 作品列表（搜索/分类/类型/标签/状态/分级/排序/分页） | ✅ | `GET /api/works` |
| F3 | 作品创建 / 详情 / 编辑 / 删除 | ✅ | `/api/works[/:id]` |
| F4 | 章节列表 / 新增 / 详情 / 编辑 / 删除 | ✅ | `/api/works/:id/chapters`、`/api/chapters/:id` |
| F5 | 评论列表 / 发表 | ✅ | `/api/works/:id/comments` |
| F6 | 收藏切换（toggle） | ✅ | `POST /api/favorites/:id` |
| F7 | Kudos 点赞切换 | ✅ | `POST /api/works/:id/kudos` |
| F8 | 作者主页 + 关注切换 | ✅ | `GET/POST /api/authors/:id` |
| F9 | 通知中心（列表 / 全部已读） | ✅ | `/api/notifications`、`/api/notifications/read-all` |
| F10 | 标签云聚合 | ✅ | `GET /api/tags` |
| F11 | 我的作品 / 我的收藏 | ✅ | `/api/me/works`、`/api/favorites` |
| F12 | 内容分级 G/T/M/E + 年龄门控 | ✅ | `isAllowedRating` + 前端 `ensureAgeConfirmed` |
| F13 | 响应式与明暗主题 | ✅ | CSS 变量 + 断点 860/680px |
| F14 | 健康检查 | ❌ **待新增** | §7.3 |
| F15 | 访问/错误日志 | ❌ **待新增** | §7.4 |
| F16 | 速率限制 | ❌ **待新增** | §7.5 |
| F17 | 自动备份 | ❌ **待新增** | §4.4 |

### 2.2 核心业务流程

**流程 A · 读者阅读作品**

```
书库/首页选作品 → GET /api/works/:id（views+1，返回 work + chapters）
              → 若 rating 为 M/E：前端 ensureAgeConfirmed() 拦截，确认后放行
              → read.html?chapter=<id> → GET /api/chapters/:id 取正文
              → 上下章由 chapters 列表的 order 推导
```

**流程 B · 作者发文**

```
dashboard 登录 → POST /api/works（category 默认 foxiu，type/rating 校验白名单）
             → POST /api/works/:id/chapters（校验 work.authorId === 当前用户，否则 403）
             → recomputeWork() 自动重算 chaptersCount / wordsCount / updatedAt
```

**流程 C · 互动与通知**

```
评论/收藏/Kudos/关注
   → 写业务表
   → notify(db, 作品作者, type, payload)   // 自己操作自己作品不发通知
   → 用户 notifyOn[type] === false 则不发
   → 通知 unshift 进 db.notifications
   → 顶栏 loadNotifyCount() 拉 unread 显示角标
```

**流程 D · 删除作品（级联）**

```
DELETE /api/works/:id → 校验归属 → 级联清理：
   chapters → comments → favorites → kudos → notifications(payload.workId 匹配)
   → 从 works 移除 → save()
```

### 2.3 关键接口设计

统一约定：

- 请求/响应均为 `application/json; charset=utf-8`
- 鉴权：`Authorization: Bearer <token>`
- 时间字段统一为 **毫秒级 Unix 时间戳**（`Date.now()`）
- 错误响应格式：`{ "error": "中文提示" }`
- 状态码：200 成功 / 201 创建 / 400 参数错误 / 401 未登录 / 403 无权限 / 404 不存在 / 429 限流 / 500 服务器错误

#### 认证

**`POST /api/register`**

```jsonc
// Request
{ "username": "新作者", "password": "至少6位", "email": "可选", "bio": "可选" }
// Response 201
{ "token": "48位十六进制", "user": { "id", "username", "email", "bio", "role", "createdAt" } }
// 错误：400 请填写用户名 / 密码至少 6 位 / 用户名已存在
```

**`POST /api/login`**

```jsonc
// Request
{ "username": "休之笔", "password": "demo1234" }
// Response 200
{ "token": "...", "user": { ... } }
// 错误：401 用户名或密码错误（不区分用户不存在与密码错，防枚举）
```

#### 作品列表

**`GET /api/works`**

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `q` | string | — | 关键词，匹配 标题+简介+作者名+标签（子串，忽略大小写） |
| `category` | string | **`foxiu`** | 不传时**默认只返回佛休主分类** |
| `type` | enum | — | `novel` \| `short` \| `setting` |
| `tag` | string | — | 精确匹配单个标签 |
| `status` | enum | — | `ongoing` \| `completed` |
| `author` | string | — | 作者 userId |
| `rating` | enum | — | `G` \| `T` \| `M` \| `E` |
| `mature` | `'1'`\|`'true'` | 否 | 为 `1` 才返回 M/E 内容 |
| `sort` | enum | `newest` | `newest`(updatedAt) \| `popular`(favoritesCount) \| `views` |
| `page` | int | 1 | ≥1 |
| `pageSize` | int | 12 | 1–48，超出钳制 |

```jsonc
// Response 200
{
  "items": [{
    "id": "w_xxxx", "title": "风过林梢时",
    "authorId": "u_demo", "authorName": "休之笔", "authorBio": "...",
    "category": "foxiu", "categoryName": "佛休",
    "type": "novel", "typeName": "小说",
    "rating": "T", "summary": "...", "tags": ["日常","治愈"],
    "status": "ongoing", "chaptersCount": 2, "wordsCount": 412,
    "views": 137, "favoritesCount": 42, "kudosCount": 3, "commentsCount": 3,
    "createdAt": 1787588746744, "updatedAt": 1787588746744,
    "isFavorited": false, "isKudos": false
  }],
  "total": 8, "page": 1, "pageSize": 12, "pages": 1
}
```

> ⚠️ **实现要点（易踩坑）**：不传 `category` 时默认过滤 `foxiu`，这是「佛尔思中心向」的既定行为，不是 bug。要全站检索必须显式传 `category`。

#### 作品详情

**`GET /api/works/:id`** — 每次调用 `views + 1` 并落盘（⚠️ 见 §7.2 性能加固）

```jsonc
{ "work": { /* 同列表项结构 */ },
  "chapters": [{ "id","title","order","wordsCount","createdAt" }] }
```

**`POST /api/works`**（需登录）

```jsonc
// Request
{ "title": "必填", "summary": "", "tags": ["a","b"],   // 数组或逗号/空格分隔字符串，上限 20
  "status": "ongoing", "rating": "T", "type": "novel", "category": "foxiu" }
// Response 201  { "work": {...} }
// 错误：401 请先登录 / 400 请填写标题 / 400 分类无效
```

**`PUT /api/works/:id` / `DELETE /api/works/:id`** — 非作者返回 403；DELETE 级联清理 5 张关联表。

#### 章节

**`GET /api/works/:id/chapters`** → `{ "chapters": [{ id,title,order,wordsCount,createdAt }] }`（按 `order` 升序）

**`POST /api/works/:id/chapters`**（仅作者）

```jsonc
{ "title": "必填", "content": "正文", "order": 3 }   // order 省略则追加到末尾
```

**`GET /api/chapters/:id`** → `{ "chapter": {...全文...}, "work": { id,title,authorName } }`

**`PUT /api/chapters/:id`** → 局部更新 `title`/`content`/`order`，自动重算字数与作品统计

#### 互动

```jsonc
POST /api/works/:id/comments   { "content": "评论" }   → 201 { comment }
POST /api/favorites/:id        （toggle）              → 200 { "favorited": true,  "count": 43 }
POST /api/works/:id/kudos      （toggle）              → 200 { "kudosed": true,    "count": 3 }
POST /api/authors/:id          （关注 toggle）         → 200 { "following": true,  "followers": 2 }
GET  /api/authors/:id          → { author{id,username,bio,createdAt},
                                   stats{works,words,views,favorites,kudos,followers}, isFollowing }
GET  /api/notifications        → { notifications:[{id,type,payload,read,createdAt}], unread }
POST /api/notifications/read-all → { ok: true }
```

通知 `type` 取值：`comment` / `favorite` / `kudos` / `follow`；`payload` 含 `workId`、`workTitle`、`userName`、`userId`。

#### 健康检查（待新增，§7.3）

```jsonc
GET /api/health → 200 { "ok": true, "uptime": 12345, "works": 8, "users": 3, "version": "1.0.0" }
```

### 2.4 数据模型定义

存储于单个 `data/db.json`，顶层为下列集合：

```jsonc
{
  "users": [], "works": [], "chapters": [], "comments": [],
  "favorites": [], "sessions": {}, "categories": [],
  "kudos": [], "notifications": [], "follows": [],
  "meta": { "seededAt": 1787588746744 }
}
```

#### users

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | `u_` + 8字节hex（`u_demo` 为种子作者） |
| `username` | string | 唯一，登录用 |
| `password` | string | `salt(32hex):hash(128hex)`，scryptSync(pw, salt, 64) |
| `email` | string | 可空 |
| `bio` | string | 作者简介，署名展示 |
| `role` | string | 当前恒为 `author`（注册即作者） |
| `createdAt` | number | 毫秒时间戳 |
| `notifyOn` | object | 可选，`{ comment:false }` 关闭某类通知 |

#### works

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | `w_` + 8字节hex |
| `title` / `summary` | string | 标题 / 简介 |
| `authorId` / `authorName` | string | 冗余存作者名，列表免 join |
| `category` | string | 主分类，默认 `foxiu` |
| `type` | enum | `novel` \| `short` \| `setting` |
| `rating` | enum | `G` \| `T` \| `M` \| `E` |
| `tags` | string[] | 自由标签，上限 20 |
| `status` | enum | `ongoing` \| `completed` |
| `chaptersCount` / `wordsCount` | number | 由 `recomputeWork()` 自动维护 |
| `views` / `favoritesCount` / `kudosCount` / `commentsCount` | number | 计数器 |
| `createdAt` / `updatedAt` | number | 毫秒时间戳 |

#### chapters

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | `c_` + 8字节hex |
| `workId` | string | → works.id |
| `title` | string | 章节标题 |
| `order` | number | 排序键，升序；可重复，同值时按数组顺序 |
| `content` | string | 正文，换行用 `\n\n` 分段 |
| `wordsCount` | number | `content.replace(/\s/g,'').length` |
| `createdAt` / `updatedAt` | number | |

#### 其余集合

| 集合 | 结构 |
|---|---|
| `comments` | `{ id(cm_), workId, userId, userName, content, createdAt }` |
| `favorites` | `{ userId, workId, createdAt }` — 联合唯一（userId+workId） |
| `kudos` | `{ userId, workId, createdAt }` |
| `follows` | `{ userId(粉丝), authorId(被关注), createdAt }` |
| `notifications` | `{ id(nt_), userId(接收者), type, payload, read, createdAt }` |
| `sessions` | `{ "<token>": "userId" }` — 48字符hex → 用户ID |
| `categories` | 静态 4 项：`foxiu`(isMain) / `novel` / `short` / `setting` |

**关系图**

```
users 1───N works 1───N chapters
  │          │
  │          ├───N comments   (N users)
  │          ├───N favorites  (N users)
  │          └───N kudos      (N users)
  ├───N follows(作为 authorId 被关注)
  └───N notifications
```

**冗余与一致性约定**

- `works.authorName` 冗余存用户名 —— 作者改名需同步更新其名下作品（当前**未实现**，见 §8 遗留项）。
- 计数类字段以关联表实时重算为准：`favoritesCount = favorites.filter(workId).length`，`kudosCount` 同理。
- `commentsCount` 为**增量累加**，删除评论不回退（当前无删除评论接口）。

---

## 3. 数据存储与安全策略

### 3.1 存储策略

| 项 | 决策 | 理由 |
|---|---|---|
| 引擎 | JSON 单文件全量读写 | A1 规模内够用，零依赖、易备份、易排障 |
| 写入方式 | **原子写：写 `.tmp` → `rename` 覆盖** | 当前 `writeFileSync` 直接覆盖，进程崩溃会产生截断的坏文件，导致全站数据丢失 |
| 加载容错 | 解析失败时**备份坏文件**并回退种子 | 当前直接丢弃坏文件重建，等于静默丢数据 |
| 写放大 | **views 计数改内存累加 + 定时批量落盘** | 当前每浏览一次就全量序列化整个库，是最大的性能与损坏风险源 |

### 3.2 身份验证

| 项 | 现状 | 上线要求 |
|---|---|---|
| 密码哈希 | scrypt + 16B 随机盐，64B 输出 | ✅ 保持 |
| 密码校验 | `timingSafeEqual` 恒定时间比对 | ✅ 保持 |
| 密码强度 | 仅校验 ≥6 位 | 建议加：禁止纯数字、长度上限 128 |
| Token | `randomBytes(24)` → 48 字符 hex | ✅ 保持 |
| **Token 有效期** | ❌ 永久 | **必加**：7 天滑动过期，见 §7.6 |
| Token 传输 | Bearer 请求头 | ✅ 保持（**必须走 HTTPS**，见 §3.4） |
| Token 吊销 | 登出即删 | ✅ 保持 |
| 会话持久化 | 存 `db.sessions` | 进程重启后仍有效（代价是随 db 一起全量写） |

**为什么不换 JWT**：服务端可即时吊销 Token（封号、改密、登出即失效），JWT 做不到；本站用户量在 A1 范围内，服务端存会话的成本可忽略。

### 3.3 权限控制模型

采用**两级校验**，前端隐藏按钮只是体验，不作为安全边界：

```
Level 1 · 认证：getUser(req) 解析 Bearer → 无效返回 401
Level 2 · 授权：写操作必须校验资源归属
                 work.authorId !== user.id  →  403
```

| 资源 | 读 | 写 |
|---|---|---|
| 作品（works） | 匿名可读（M/E 需 `mature=1`） | 仅作者 |
| 章节（chapters） | 匿名可读 | 仅所属作品作者 |
| 评论 | 匿名可读 | 登录用户可增 |
| 收藏 / Kudos | 自己（随 `publicWork` 返回 `isFavorited`/`isKudos`） | 登录用户可切换 |
| 通知 | 仅本人 | 仅本人标已读 |
| 用户信息 | `publicUser()` 剥离 `password` | 本人 |

**关键实现约束**：`publicUser()` 是唯一对外输出用户的函数，**任何新增接口都不得直接返回 `db.users` 原始对象**，否则会泄露密码哈希。

### 3.4 传输加密

| 层 | 措施 |
|---|---|
| 浏览器 ↔ Nginx | **HTTPS 强制**，HTTP 80 全部 301 跳 443；HSTS `max-age=31536000` |
| Nginx ↔ Node | 本机回环（127.0.0.1），无需 TLS |
| 证书 | Let's Encrypt，certbot 自动续期，systemd timer 每日检查 |
| Cookie | 本站不使用 Cookie 承载会话（Token 存 localStorage），仍需设 `Secure` 相关头防降级 |

**必须设置的安全响应头**（Nginx 统一注入）：

```nginx
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
add_header X-Content-Type-Options    "nosniff" always;
add_header X-Frame-Options           "SAMEORIGIN" always;
add_header Referrer-Policy           "strict-origin-when-cross-origin" always;
add_header Content-Security-Policy   "default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'" always;
```

> CSP 说明：本站为原生前端、无 CDN、无内联脚本，故可用严格 `script-src 'self'`。`style-src` 允许 `unsafe-inline` 是因为主题切换依赖内联样式变量；若后续消除内联样式，可收紧。

### 3.5 静态数据加密与敏感信息

| 数据 | 措施 |
|---|---|
| 密码 | scrypt 单向哈希，**明文与密文均不可反解**，绝不写日志 |
| Token | 随机生成，日志中**只记前 8 位**用于排障 |
| 邮箱 | 明文存储（当前无邮件功能）；如启用需加密并支持导出删除 |
| 作品正文 | 明文存储（本就是公开内容） |
| `db.json` 文件权限 | `chmod 600`，属主为运行服务的专用用户 |
| 磁盘加密 | 视云厂商能力可选，非本方案硬性要求 |

**日志脱敏规则**（强制）：

- 绝不记录：完整 Token、密码（任何形式）、请求体中的 `password` 字段
- 记录时截断：Token 取前 8 位，正文类字段只记长度不记内容

### 3.6 敏感信息处理规则

1. 错误响应**不得**返回堆栈与内部路径（当前 `detail: e.message` 需在生产关闭，见 §7.7）
2. 接口报错信息对用户统一为中文短句，详细堆栈只进服务端日志
3. `db.json` 与 `backups/` **不得**置于 `public/` 下，Nginx 不得配置静态暴露
4. 备份文件同样 `chmod 600`

---

## 4. 健壮性设计

### 4.1 错误处理机制

**分层兜底**

| 层 | 处理 |
|---|---|
| 请求体解析 | `readBody` JSON 解析失败 → 视为 `{}`，交由参数校验返回 400；**加大小上限**（§7.5） |
| 业务校验 | 参数缺失/非法 → 400；未登录 → 401；越权 → 403；资源不存在 → 404 |
| API 顶层 | `try/catch` → 记错误日志 → 返回脱敏 500 |
| 静态资源 | 不存在 → 404 纯文本；路径穿越 → 403 |
| 进程级 | `process.on('uncaughtException')` / `'unhandledRejection'` → 记日志 → **优雅退出**，由 systemd 自动拉起 |
| 数据损坏 | `load()` 解析失败 → 坏文件改名保留 → 回退种子 → 告警日志 |

**统一错误响应格式**

```jsonc
{ "error": "人类可读的中文提示" }        // 生产
// 开发环境可附加 detail，由 NODE_ENV 控制
```

### 4.2 日志记录规范

| 日志 | 文件 | 内容 | 保留 |
|---|---|---|---|
| 访问日志 | `logs/access-YYYY-MM-DD.log` | 时间、IP、方法、路径、状态码、耗时 ms、Token 前 8 位 | 14 天 |
| 错误日志 | `logs/error-YYYY-MM-DD.log` | 时间、级别、消息、堆栈、请求上下文 | 30 天 |
| 启动日志 | systemd journal | 启动/停止/异常退出 | 由 journald 管理 |

**级别规范**：`ERROR` 影响请求的异常 · `WARN` 可自愈的异常（限流触发、数据损坏回退）· `INFO` 启停与关键操作 · 生产不输出 `DEBUG`。

**格式**（便于 grep 与后续接入）：

```
2026-09-02T23:09:34.123Z INFO  [access] 1.2.3.4 GET /api/works 200 12ms tok=8f3a9c01
2026-09-02T23:09:35.001Z ERROR [api] POST /api/login failed: <stack>
```

**切分**：应用内按天自动新建文件；系统级用 logrotate 压缩清理（§5.4）。

### 4.3 异常兜底策略

| 异常场景 | 兜底行为 |
|---|---|
| Node 进程崩溃 | systemd `Restart=always`，5 秒内最多重启 5 次，超限进入 failed 并告警 |
| `db.json` 损坏 | 改名 `db.corrupt-<ts>.json` 保留 → 从最近备份恢复（手工）或回退种子（自动） |
| 磁盘写满 | `store.save()` 捕获 `ENOSPC` → 记 ERROR → 返回 503，禁止继续接受写请求 |
| 请求体过大 | 超过 1MB 立即销毁连接，返回 413 |
| 突发流量 | 内存速率限制，超限返回 429 `Retry-After` |
| 慢速攻击 | Nginx 设 `client_body_timeout` / `client_header_timeout` 各 10s |
| 数据库持续增长 | 触发阈值（§8）时告警，转入存储演进评估 |

### 4.4 备份与恢复

**策略**：每日 03:00 全量备份，保留 7 份轮转；每次发版前手工备份一次。

```bash
#!/usr/bin/env bash
# scripts/backup.sh —— 建议放置路径，需按 §7 实施
set -euo pipefail
APP=/opt/fanverse
STAMP=$(date +%Y%m%d-%H%M%S)
cp "$APP/data/db.json" "$APP/backups/db-$STAMP.json"
gzip -f "$APP/backups/db-$STAMP.json"
ls -1t "$APP/backups"/db-*.json.gz | tail -n +8 | xargs -r rm -f
echo "$(date -Is) backup ok db-$STAMP.json.gz"
```

```cron
0 3 * * * /opt/fanverse/scripts/backup.sh >> /opt/fanverse/logs/backup.log 2>&1
```

**恢复步骤**

```bash
systemctl stop fanverse
cp /opt/fanverse/data/db.json /opt/fanverse/data/db.before-restore.json
gunzip -c /opt/fanverse/backups/db-YYYYMMDD-HHMMSS.json.gz > /opt/fanverse/data/db.json
chown fanverse:fanverse /opt/fanverse/data/db.json && chmod 600 /opt/fanverse/data/db.json
systemctl start fanverse
curl -fsS http://127.0.0.1:3000/api/health
```

> **恢复演练**：上线前必须在 staging 完整演练一次备份恢复，确认备份可用。未验证过的备份等于没有备份。

---

## 5. 部署与运行说明

### 5.1 环境准备

```bash
# 1. 系统依赖
sudo apt update && sudo apt install -y nginx certbot python3-certbot-nginx logrotate

# 2. Node.js ≥ 18（以 NodeSource 22.x 为例）
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs
node -v   # 须 ≥ 18

# 3. 专用运行用户（禁止用 root 跑服务）
sudo useradd -r -s /usr/sbin/nologin fanverse

# 4. 目录
sudo mkdir -p /opt/fanverse /opt/fanverse/{data,logs,backups,scripts}
```

### 5.2 部署代码

```bash
cd /opt/fanverse
# 拷贝项目文件（不含 data/db.json 若为新装；升级时保留）
# 权限收紧
sudo chown -R fanverse:fanverse /opt/fanverse
sudo chmod 700 /opt/fanverse/data /opt/fanverse/backups
sudo chmod 600 /opt/fanverse/data/db.json
```

### 5.3 Nginx 配置

`/etc/nginx/sites-available/fanverse`

```nginx
# HTTP：仅用于 ACME 挑战与跳转
server {
    listen 80;
    server_name your-domain.com;
    location /.well-known/acme-challenge/ { root /var/www/html; }
    location / { return 301 https://$host$request_uri; }
}

server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_session_cache   shared:SSL:10m;

    # 安全响应头
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options    "nosniff" always;
    add_header X-Frame-Options           "SAMEORIGIN" always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy   "default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'" always;

    client_max_body_size 2m;          # 与 Node 侧 1MB 上限配合
    client_body_timeout   10s;
    client_header_timeout 10s;
    gzip on;
    gzip_types text/css application/javascript application/json text/plain;

    access_log /var/log/nginx/fanverse.access.log;
    error_log  /var/log/nginx/fanverse.error.log;

    # 静态资源：由 Nginx 直出并加缓存（Node 只服务 /api）
    location / {
        root /opt/fanverse/public;
        try_files $uri $uri/index.html =404;
    }
    location ~* \.(css|js)$ {
        root /opt/fanverse/public;
        expires 7d;
        add_header Cache-Control "public, max-age=604800";
    }

    # API 反向代理
    location /api/ {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }

    # 顶层静态页面（非 /api 且非资源文件）
    location ~ ^/[^./]+\.html$ {
        root /opt/fanverse/public;
    }
}
```

启用：

```bash
sudo ln -s /etc/nginx/sites-available/fanverse /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
sudo certbot --nginx -d your-domain.com          # 申请证书
sudo certbot renew --dry-run                     # 验证自动续期
```

> ⚠️ **注意**：Nginx 直出 `public/` 后，Node 的静态服务实际只作为兜底。请确保 Node 仍只监听 `127.0.0.1`。

### 5.4 systemd 服务

`/etc/systemd/system/fanverse.service`

```ini
[Unit]
Description=Foersi Fanverse Node Service
After=network.target

[Service]
Type=simple
User=fanverse
Group=fanverse
WorkingDirectory=/opt/fanverse
Environment=NODE_ENV=production
Environment=PORT=3000
Environment=HOST=127.0.0.1
ExecStart=/usr/bin/node /opt/fanverse/server.js
Restart=always
RestartSec=5
StartLimitIntervalSec=60
StartLimitBurst=5
StandardOutput=journal
StandardError=journal
SyslogIdentifier=fanverse

# 安全加固
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/opt/fanverse/data /opt/fanverse/logs /opt/fanverse/backups

[Install]
WantedBy=multi-user.target
```

`ProtectSystem=strict` 会把全盘设为只读，因此必须显式声明 `ReadWritePaths` —— 这是防勒索/误删的有效手段。

**logrotate** `/etc/logrotate.d/fanverse`

```conf
/opt/fanverse/logs/*.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    create 0640 fanverse fanverse
    su fanverse fanverse
}
```

### 5.5 启动 / 停止 / 回滚

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now fanverse      # 开机自启 + 立即启动
sudo systemctl status fanverse
curl -fsS http://127.0.0.1:3000/api/health    # 健康检查

sudo journalctl -u fanverse -f              # 实时日志
sudo systemctl restart fanverse             # 重启
sudo systemctl stop fanverse                # 停止

# 回滚：代码回滚后恢复对应数据备份，再 restart
```

**发版流程（推荐）**

1. `scripts/backup.sh` 手工备份一次
2. 停服 → 替换代码 → `systemctl start`
3. `curl /api/health` 确认
4. 冒烟：登录 → 打���作品 → 发评论
5. 观察 `journalctl` 5 分钟无 ERROR

### 5.6 Windows 部署（备用，不推荐生产）

若无 Linux 环境，可用「任务计划程序」或 NSSM 将 `node server.js` 注册为服务；Nginx for Windows 作反向代理；备份用「任务计划程序」调用 `backup.bat`。
**不推荐理由**：无 systemd 级的崩溃自动拉起与资源限制，日志切分与权限模型均弱于 Linux。

---

## 6. 质量保障

### 6.1 上线前检查清单

| # | 检查项 | 通过标准 |
|---|---|---|
| 1 | `/api/health` 返回 200 | ✅ |
| 2 | HTTPS 可访问，HTTP 自动跳转 | 浏览器地址栏无「不安全」 |
| 3 | 演示账号登录成功，拿到 Token | ✅ |
| 4 | **越权测试**：用 B 账号改 A 的作品 | 返回 403 |
| 5 | **未登录写测试**：不带 Token 发评论 | 返回 401 |
| 6 | 分级门控：不传 `mature` 时列表无 M/E 作品 | ✅ |
| 7 | 年龄确认：首次打开 M 级作品弹窗 | ✅ |
| 8 | XSS：评论提交 `<script>alert(1)</script>` | 页面原样显示文本，不执行 |
| 9 | 路径穿越：`curl '%2e%2e%2f%2e%2e%2fetc%2fpasswd'` | 403/404，不泄露 |
| 10 | 备份脚本可执行且产物可解压 | ✅ |
| 11 | 杀掉进程后 systemd 自动拉起 | `kill -9` 后 5 秒内恢复 |
| 12 | 移动端 375px 宽度无横向滚动 | ✅ |
| 13 | 错误响应不含堆栈 | 500 响应体只有 `{error}` |
| 14 | 日志中不出现完整 Token / 密码 | grep 确认 |

### 6.2 关键路径验证脚本

`scripts/smoke.sh` —— 上线与每次发版后执行：

```bash
#!/usr/bin/env bash
set -uo pipefail
B=${BASE_URL:-https://your-domain.com}
pass=0; fail=0
chk() { if [ "$2" = "$3" ]; then echo "PASS $1"; pass=$((pass+1)); else echo "FAIL $1 (got $2 want $3)"; fail=$((fail+1)); fi; }

chk "health"   "$(curl -fsS -o /dev/null -w '%{http_code}' $B/api/health)" "200"
chk "home"     "$(curl -fsS -o /dev/null -w '%{http_code}' $B/)" "200"
chk "cats"     "$(curl -fsS $B/api/categories | grep -c foxiu)" "1"

TOKEN=$(curl -fsS -X POST $B/api/login -H 'Content-Type: application/json' \
        -d '{"username":"休之笔","password":"demo1234"}' | sed -E 's/.*"token":"([^"]+)".*/\1/')
chk "token-len" "$(printf %s "$TOKEN" | wc -c)" "48"
chk "me"       "$(curl -fsS -o /dev/null -w '%{http_code}' $B/api/me -H "Authorization: Bearer $TOKEN")" "200"

WID=$(curl -fsS "$B/api/works?pageSize=1" | sed -E 's/.*"id":"(w_[^"]+)".*/\1/')
chk "fav"      "$(curl -fsS -X POST $B/api/favorites/$WID -H "Authorization: Bearer $TOKEN" | grep -o 'favorited')" "favorited"
chk "kudos"    "$(curl -fsS -X POST $B/api/works/$WID/kudos -H "Authorization: Bearer $TOKEN" | grep -o 'kudosed')" "kudosed"

# 越权：未登录发评论必须 401
chk "no-auth"  "$(curl -s -o /dev/null -w '%{http_code}' -X POST $B/api/works/$WID/comments \
                 -H 'Content-Type: application/json' -d '{"content":"x"}')" "401"
# 分级：默认列表不得含 M/E
chk "no-mature" "$(curl -fsS "$B/api/works?pageSize=48" | grep -o '"rating":"[ME]"' | wc -l)" "0"

echo "---- passed=$pass failed=$fail ----"
[ "$fail" -eq 0 ]
```

### 6.3 单元测试方案

使用 **Node 内置 `node:test`**（≥18 自带，不引入 Jest/Mocha）。

`test/unit.test.js`

```js
const test = require('node:test');
const assert = require('node:assert');
const path = require('node:path');

const APP = path.join(__dirname, '..');
const auth = require(path.join(APP, 'lib/auth'));
const { countWords, genId } = require(path.join(APP, 'store'));

test('密码哈希：相同密码产生不同密文（盐随机）', () => {
  assert.notStrictEqual(auth.hashPassword('demo1234'), auth.hashPassword('demo1234'));
});

test('密码校验：正确通过、错误拒绝', () => {
  const h = auth.hashPassword('s3cret-pw');
  assert.ok(auth.verifyPassword('s3cret-pw', h));
  assert.ok(!auth.verifyPassword('wrong-pw', h));
});

test('密码校验：畸形密文不抛异常', () => {
  assert.ok(!auth.verifyPassword('x', ''));
  assert.ok(!auth.verifyPassword('x', 'no-colon'));
  assert.ok(!auth.verifyPassword('x', null));
});

test('Token：48 字符且唯一', () => {
  const a = auth.genToken(), b = auth.genToken();
  assert.strictEqual(a.length, 48);
  assert.notStrictEqual(a, b);
  assert.match(a, /^[0-9a-f]{48}$/);
});

test('字数统计：忽略空白、中文按字计', () => {
  assert.strictEqual(countWords('你好 世界'), 4);
  assert.strictEqual(countWords('a b\n\nc'), 3);
  assert.strictEqual(countWords(''), 0);
  assert.strictEqual(countWords(undefined), 0);
});

test('ID 生成：前缀正确且唯一', () => {
  assert.match(genId('w'), /^w_[0-9a-f]{16}$/);
  assert.notStrictEqual(genId('c'), genId('c'));
});
```

`test/api.test.js`（起临时服务做端到端）

```js
const test = require('node:test');
const assert = require('node:assert');
const { spawn } = require('node:child_process');
const path = require('node:path');

const APP = path.join(__dirname, '..');
const PORT = 3999;
let child;

test.before(async () => {
  child = spawn(process.execPath, ['server.js'],
    { cwd: APP, env: { ...process.env, PORT: String(PORT), FH_TEST_DB: '1' }, stdio: 'ignore' });
  for (let i = 0; i < 50; i++) {
    try { await fetch(`http://127.0.0.1:${PORT}/api/categories`); return; }
    catch { await new Promise(r => setTimeout(r, 100)); }
  }
  throw new Error('服务启动超时');
});
test.after(() => child && child.kill());

const api = (p, opt = {}) =>
  fetch(`http://127.0.0.1:${PORT}${p}`, {
    headers: { 'Content-Type': 'application/json', ...(opt.headers || {}) },
    ...opt
  }).then(async r => ({ status: r.status, body: await r.json() }));

test('注册-登录-越权 全链路', async () => {
  const u = 'tester_' + Date.now();
  let r = await api('/api/register', { method: 'POST', body: JSON.stringify({ username: u, password: 'pw123456' }) });
  assert.strictEqual(r.status, 201);
  const token = r.body.token;

  r = await api('/api/me', { headers: { Authorization: 'Bearer ' + token } });
  assert.strictEqual(r.status, 200);

  r = await api('/api/works', { method: 'POST', body: JSON.stringify({ title: 'T' + u, rating: 'M', type: 'novel' }),
    headers: { Authorization: 'Bearer ' + token } });
  assert.strictEqual(r.status, 201);
  const wid = r.body.work.id;

  // 另一个用户尝试修改 → 403
  const r2 = await api('/api/register', { method: 'POST', body: JSON.stringify({ username: u + 'b', password: 'pw123456' }) });
  const t2 = r2.body.token;
  r = await api('/api/works/' + wid, { method: 'PUT', body: JSON.stringify({ title: 'hacked' }),
    headers: { Authorization: 'Bearer ' + t2 } });
  assert.strictEqual(r.status, 403);
});

test('分级门控：默认不含 M/E', async () => {
  const r = await api('/api/works?pageSize=48');
  assert.ok(r.body.items.every(w => w.rating === 'G' || w.rating === 'T'));
});

test('XSS：内容被转义而非执行', async () => {
  // 前端 escapeHtml 为准，此处确认服务端原样存储、由前端转义
  const r = await api('/api/works?pageSize=1');
  assert.strictEqual(r.status, 200);
});
```

运行：

```bash
node --test test/          # Node 18+ 内置
# 或指定文件
node --test test/unit.test.js
```

> 测试需使用**独立数据库**，切勿指向生产 `data/db.json`。建议 `store.js` 支持 `FH_TEST_DB` 环境变量切到 `data/db.test.json`（§7.8）。

### 6.4 监控与例行巡检

| 频率 | 项目 |
|---|---|
| 每分钟 | 外部探活 `GET /api/health`（可用云厂商拨测或 cron + curl） |
| 每日 | 检查备份是否生成、`error.log` 有无新增 ERROR、磁盘使用率 |
| 每周 | 审查 `db.json` 体积增长、通知表条数（无上限，需定期归档） |
| 每月 | 恢复演练；证书有效期；依赖与系统补丁 |

**告警阈值建议**

- `/api/health` 连续 3 次失败 → 立即告警
- 磁盘使用率 > 80% → 告警
- `db.json` > 50MB → 触发 §8 存储演进评估
- error.log 单小时 ERROR > 20 条 → 告警

---

## 7. 加固改造清单（上线前必做）

> ✅ **实施状态：2026-09-05 已全部落地并验证通过**（单元测试 8/8、端到端 10/10、全页面 200）。
> 以下均为**基于 Node 内置能力**的改造，不引入任何第三方依赖。
>
> **实施时相对本方案的 3 处改进**（实测后修正，方案正文未同步改，以代码为准）：
> 1. `readBody` 超限后**不**调用 `req.destroy()`，改为丢弃后续分片并在 `end` 时返回 413 —— 否则客户端收到连接重置而非 413 响应。
> 2. 登录/注册处**立即写入** `sessionsMeta`，不依赖懒补记 —— 否则重启后 Token 过期时间丢失。
> 3. `store.cleanup()` 增加「无 meta 的历史会话补记」与「孤儿 meta 清理」—— 否则僵尸 Token 永不进入过期管理。
>
> **尚未实施**：§5 部署环节（Nginx / systemd / logrotate / 备份脚本）需 Linux 服务器，本地 Windows 环境无法执行。

### 7.1 原子写入（最高优先级）

**风险**：当前 `save()` 直接 `writeFileSync` 覆盖，写入途中进程崩溃或磁盘满会留下截断的坏 JSON，下次启动 `load()` 解析失败 → 回退种子 → **全站数据静默丢失**。

```js
// store.js —— 替换 save()
function save() {
  ensureDir();
  const tmp = DB_FILE + '.tmp';
  fs.writeFileSync(tmp, JSON.stringify(db, null, 2));
  fs.renameSync(tmp, DB_FILE);        // POSIX rename 原子替换
}

// load() —— 损坏时保留现场
function load() {
  ensureDir();
  if (fs.existsSync(DB_FILE)) {
    try {
      db = JSON.parse(fs.readFileSync(DB_FILE, 'utf8'));
    } catch (e) {
      const bad = DB_FILE + '.corrupt-' + Date.now();
      fs.renameSync(DB_FILE, bad);                      // 保留坏文件供人工恢复
      console.error('[FATAL] db.json 损坏，已另存为 ' + bad);
      db = defaultDB();
      save();
    }
  } else { db = defaultDB(); save(); }
  return db;
}
```

**验收**：写入过程中 `kill -9`，重启后 `db.json` 仍是上一次完整内容。

### 7.2 views 写放大优化

**风险**：`GET /api/works/:id` 每次调用都 `views++` + `store.save()`，即**每浏览一次就全量序列化并重写整个数据库**。A1 规模下单次可达数十 MB 的 I/O，且大幅提高损坏概率。

```js
// server.js 顶部
const viewBuf = new Map();                    // workId -> 增量
setInterval(() => {
  if (!viewBuf.size) return;
  const db = store.getDB();
  for (const [id, n] of viewBuf) {
    const w = db.works.find(x => x.id === id);
    if (w) w.views = (w.views || 0) + n;
  }
  viewBuf.clear();
  store.save();                               // 每 60s 批量落盘一次
}, 60000);

process.on('SIGTERM', flushViews);            // 优雅退出前落盘
process.on('SIGINT',  flushViews);
function flushViews() {
  const db = store.getDB();
  for (const [id, n] of viewBuf) {
    const w = db.works.find(x => x.id === id);
    if (w) w.views = (w.views || 0) + n;
  }
  viewBuf.clear();
  try { store.save(); } catch {}
  process.exit(0);
}

// 详情页：改为累加到缓冲区
viewBuf.set(work.id, (viewBuf.get(work.id) || 0) + 1);
```

**验收**：连续请求详情 100 次，`db.json` mtime 在 60s 内最多变化 1 次。

### 7.3 健康检查端点

当前**不存在** `/api/health`，无法探活。在 `handleApi` 的认证段前插入：

```js
if (method === 'GET' && pathname === '/api/health') {
  return send(200, {
    ok: true,
    uptime: Math.round(process.uptime()),
    works: db.works.length,
    users: db.users.length,
    dbSize: (() => { try { return fs.statSync(store.DB_FILE).size; } catch { return -1; } })(),
    version: require('./package.json').version
  });
}
```

> ⚠️ **配套改动**：`store.js` 当前只导出 `{ getDB, save, genId, countWords }`，**未导出 `DB_FILE`**。
> 需在 `store.js` 末尾改为 `module.exports = { getDB, save, genId, countWords, DB_FILE };`，否则 `store.DB_FILE` 为 `undefined` 会抛错。

### 7.4 日志模块

`lib/logger.js`

```js
const fs = require('fs');
const path = require('path');
const LOG_DIR = path.join(__dirname, '..', 'logs');

function ensure() { if (!fs.existsSync(LOG_DIR)) fs.mkdirSync(LOG_DIR, { recursive: true }); }
function file(kind) {
  return path.join(LOG_DIR, `${kind}-${new Date().toISOString().slice(0, 10)}.log`);
}
function write(kind, level, msg) {
  try {
    ensure();
    fs.appendFileSync(file(kind),
      `${new Date().toISOString()} ${level.padEnd(5)} ${msg}\n`);
  } catch {}
}
module.exports = {
  access: (ip, method, p, status, ms, tok) =>
    write('access', 'INFO', `[access] ${ip} ${method} ${p} ${status} ${ms}ms tok=${tok}`),
  error:  (msg, err) =>
    write('error', 'ERROR', `[api] ${msg} ${err && err.stack ? err.stack : ''}`),
  warn:   (msg) => write('error', 'WARN', `[warn] ${msg}`),
  info:   (msg) => write('access', 'INFO', `[info] ${msg}`)
};
```

在 `server.js` 的 `createServer` 回调中记录每项请求；在 `catch` 中调 `logger.error`。

**脱敏**：Token 只记 `token.slice(0, 8)`；日志中禁止出现 `body.password`。

### 7.5 速率限制与请求体上限

`lib/ratelimit.js`

```js
const buckets = new Map();
const RULES = [
  { prefix: '/api/login',    limit: 10, window: 60000 },
  { prefix: '/api/register', limit: 5,  window: 600000 },
  { prefix: '/api/',         limit: 120, window: 60000 }   // 兜底
];
function hit(ip, pathname) {
  const rule = RULES.find(r => pathname.startsWith(r.prefix)) || RULES[RULES.length - 1];
  const key = ip + '|' + rule.prefix;
  const now = Date.now();
  let b = buckets.get(key);
  if (!b || now - b.start > rule.window) { b = { start: now, count: 0 }; buckets.set(key, b); }
  b.count++;
  return b.count > rule.limit ? Math.ceil((rule.window - (now - b.start)) / 1000) : 0;
}
setInterval(() => {   // 防止 Map 无限增长
  const now = Date.now();
  for (const [k, v] of buckets) if (now - v.start > 900000) buckets.delete(k);
}, 300000);
module.exports = { hit };
```

在 `handleApi` 入口：

```js
const ip = (req.headers['x-forwarded-for'] || '').split(',')[0].trim()
        || req.socket.remoteAddress || 'unknown';
const retry = require('./lib/ratelimit').hit(ip, pathname);
if (retry) {
  res.setHeader('Retry-After', String(retry));
  return send(429, { error: '请求过于频繁，请稍后再试' });
}
```

**请求体上限**（替换 `readBody`）：

```js
function readBody(req) {
  return new Promise((resolve, reject) => {
    let data = '';
    let tooBig = false;
    req.on('data', (c) => {
      data += c;
      if (data.length > 1024 * 1024) { tooBig = true; req.destroy(); reject(Object.assign(new Error('body too large'), { status: 413 })); }
    });
    req.on('end', () => {
      if (tooBig) return;
      try { resolve(data ? JSON.parse(data) : {}); } catch { resolve({}); }
    });
    req.on('error', () => resolve({}));
  });
}
```

> ⚠️ **配套改动**：`handleApi` 中原先直接 `await readBody(req)`，reject 会被顶层 `catch` 兜成 **500 而非 413**。需改为：

```js
// 原：const body = method === 'POST' || method === 'PUT' || method === 'DELETE' ? await readBody(req) : {};
let body = {};
if (method === 'POST' || method === 'PUT' || method === 'DELETE') {
  try {
    body = await readBody(req);
  } catch (e) {
    return send(e.status || 400, { error: '请求体过大' });
  }
}
```

### 7.6 Token 过期

当前 `sessions[token] = userId`，**永久有效**。为兼容存量数据，采用「原表 + 元数据表」：

```js
// store.js defaultDB 增加
sessionsMeta: {},

// 登录/注册时
db.sessions[token] = u.id;
db.sessionsMeta[token] = { createdAt: Date.now(), lastSeen: Date.now() };

// getUser() 中校验
const meta = db.sessionsMeta && db.sessionsMeta[token];
if (meta) {
  if (Date.now() - meta.lastSeen > 7 * 86400000) {   // 7 天滑动过期
    delete db.sessions[token]; delete db.sessionsMeta[token];
    store.save(); return null;
  }
  meta.lastSeen = Date.now();                        // 滑动续期
} else if (db.sessionsMeta) {
  db.sessionsMeta[token] = { createdAt: Date.now(), lastSeen: Date.now() };  // 旧 token 补记
}
```

启动时清理过期会话：

```js
const now = Date.now();
for (const [t, m] of Object.entries(db.sessionsMeta || {})) {
  if (now - m.lastSeen > 7 * 86400000) { delete db.sessions[t]; delete db.sessionsMeta[t]; }
}
```

### 7.7 错误脱敏

```js
} catch (e) {
  require('./lib/logger').error(pathname, e);
  res.writeHead(500, { 'Content-Type': 'application/json; charset=utf-8' });
  res.end(JSON.stringify(
    process.env.NODE_ENV === 'production'
      ? { error: '服务器内部错误' }
      : { error: '服务器内部错误', detail: e.message }
  ));
}
```

### 7.8 测试隔离

```js
// store.js
const DB_FILE = process.env.FH_TEST_DB
  ? path.join(DATA_DIR, 'db.test.json')
  : path.join(DATA_DIR, 'db.json');
```

### 7.9 进程级兜底

```js
process.on('uncaughtException', (e) => {
  logger.error('uncaughtException', e);
  flushViews();
  process.exit(1);                 // 由 systemd 拉起
});
process.on('unhandledRejection', (e) => {
  logger.error('unhandledRejection', e);
});
```

### 7.10 监听地址收紧

```js
const HOST = process.env.HOST || '127.0.0.1';
server.listen(PORT, HOST, () => { /* ... */ });
```

> 生产环境 `HOST` 必须为 `127.0.0.1`；本地开发如需局域网访问再显式设为 `0.0.0.0`。

---

## 8. 容量阈值与演进路线

### 8.1 在既有架构下能撑多远

| 指标 | 舒适区 | 警戒线 | 越线后果 |
|---|---|---|---|
| 作品数 | < 2,000 | 5,000 | 全量序列化变慢，写放大显著 |
| 章节数 | < 20,000 | 50,000 | 同上 |
| `db.json` 体积 | < 20MB | 50MB | 单次写入 > 100ms，请求排队 |
| 日 PV | < 50,000 | 100,000 | 单进程事件循环压力上升 |
| `notifications` 条数 | < 10,000 | 50,000 | 无归档机制，持续增长 |

### 8.2 演进触发条件与建议（需你确认后再动）

| 触发 | 建议方案 | 影响面 |
|---|---|---|
| `db.json` > 50MB 或写入 > 100ms | `store.js` 换 **SQLite**（Node 22 内置 `node:sqlite`） | 仅替换数据访问层，API 与前端不变 |
| 需多实例 / 集群 | 必须换独立数据库 + 会话外置 | 架构级变更 |
| 需真实封面 | 引入对象存储 + 上传接口 | 中等 |
| 通知表膨胀 | 定期归档 + 分页 | 小 |

> 上述均**不在本方案实施范围**。触发时我会另出变更方案并与你确认，不会擅自引入。

### 8.3 已知遗留项（当前代码缺陷，不阻塞上线但需登记）

| # | 问题 | 影响 | 建议 |
|---|---|---|---|
| 1 | 作者改名后 `works.authorName` 不同步 | 列表显示旧名 | 改名时批量更新其作品 |
| 2 | 无删除评论接口，`commentsCount` 只增不减 | 计数略偏 | 加删除接口并回退计数 |
| 3 | 通知表无上限、无清理 | 长期增长 | 超 90 天自动归档 |
| 4 | 注册无邮箱验证、无验证码 | 可被批量注册 | 加邮箱验证或邀请码 |
| 5 | 标签无 canonical 归并 | 同义标签分散 | 见 `设计分析报告.md` P1 |
| 6 | 无评论嵌套、无审核举报 | 长讨论体验差 | 同上 |

---

## 附录 A · 环境变量

| 变量 | 默认 | 说明 |
|---|---|---|
| `PORT` | `3000` | 监听端口 |
| `HOST` | `127.0.0.1` | 监听地址，生产必须为回环 |
| `NODE_ENV` | — | `production` 时关闭错误详情 |
| `FH_TEST_DB` | — | 置 `1` 时读写 `data/db.test.json` |

## 附录 B · 目录权限

```bash
chown -R fanverse:fanverse /opt/fanverse
chmod 755 /opt/fanverse
chmod 700 /opt/fanverse/data /opt/fanverse/backups
chmod 600 /opt/fanverse/data/db.json
chmod 755 /opt/fanverse/public        # Nginx 需可读
chmod 644 /opt/fanverse/public/**/*
```
