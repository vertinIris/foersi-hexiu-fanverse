# 佛尔思和休 · 同人作品站 — 完整方案

> 一个服务于「佛尔思和休」同人创作的在线阅读与作家创作平台。覆盖作品发布、章节式阅读、检索发现、社交互动（评论/收藏/标签）与作家后台管理，并采用响应式设计兼顾 PC 与移动端。

> 📌 **现状提示**：本文档记录的是**初版设计**。站点后续已迭代为「佛尔思 · 同人创作平台」（佛尔思中心向），新增内容分级与年龄门控、Kudos、通知中心、作者主页与关注、角色志、版权与作者权益声明等能力；**作品类型已由 5 类精简为 3 类**（小说 / 短篇 / 设定，诗歌散文并入「短篇」，剧本脚本取消）。
> 演进后的完整现状见 `功能完善总结.md`；优化诊断与 P0/P1/P2 待办见 `设计分析报告.md`。

---

## 1. 需求复盘与补充

你给出的核心模块：

| 你的需求 | 落地情况 |
| --- | --- |
| 作品分类与检索 | ✅ 佛休主分类 + 小说/短篇/设定 3 类作品类型 + 标签 + 关键词搜索 + 分级/状态/排序/分页筛选 |
| 章节式阅读界面 | ✅ 章节目录 + 独立阅读页（上下章、章节跳转、字号/主题调节） |
| 作家注册与作品管理后台 | ✅ 注册/登录 + 作家后台（作品 CRUD、章节管理） |
| 上传与编辑功能 | ✅ 作品信息与正文的创建、修改、删除 |

我在原需求基础上**补充**的模块（已一并实现）：

1. **用户评论** —— 作品详情页评论区，登录用户可发表、按时间倒序展示。
2. **收藏系统** —— 一键收藏/取消（toggle），作家可看收藏数，用户可在后台查看「我的收藏」。
3. **Tagging 标签系统** —— 作品自由打标签，自动聚合为「标签云」，支持按标签检索与发现。
4. **热门 / 排序发现** —— 按最新更新、最受欢迎（收藏数）、最多阅读三种维度排序。
5. **阅读体验增强** —— 阅读字号调节、明/暗主题切换、阅读进度友好布局。
6. **作家身份与权限** —— 注册即作者；作品/章节的编辑、删除严格校验归属，杜绝越权。
7. **安全与合规** —— 密码 scrypt 哈希 + 随机盐、token 会话鉴权、用户输入 HTML 转义（防 XSS）、私密操作服务端二次校验。

---

## 2. 技术选型与理由

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 后端 | **纯 Node.js `http`**，零第三方依赖 | 免 `npm install`、免原生编译，开箱即跑、可验证、易交付 |
| 存储 | **JSON 文件**（`data/db.json`） | 演示与中小规模足够，结构清晰、零运维；代码层已封装 `store`，将来可平滑替换为 SQLite/PostgreSQL |
| 前端 | **原生 HTML/CSS/JS**（无框架、无 CDN） | 自包含、离线可运行、无构建步骤；响应式用 CSS Grid/Flex + 媒体查询 |

> 为什么不用 Express / 数据库：本项目目标是**可立即运行、可被你直接查看的成品**。零依赖方案消除环境差异带来的「跑不起来」风险；数据访问层已抽象，后续接真实数据库只需替换 `store.js`。

---

## 3. 系统架构

```
浏览器 (多页面 SPA 风格)
  index / browse / work / read / auth / dashboard
  author / notifications / characters / about / legal
        │  fetch('/api/...')  +  Bearer Token
        ▼
Node http Server (server.js)
  ├─ 路由分发 (handleApi)
  ├─ 鉴权 (lib/auth.js: scrypt + token)
  ├─ 业务逻辑 (作品/章节/评论/收藏/分类/标签)
  └─ 静态资源服务 (public/)
        │
        ▼
JSON 存储 (data/db.json)  ← store.js 统一读写
```

- 前端为**多页面 + 共享 `common.js`**（导航、API 客户端、主题、toast），各页独立脚本。
- 后端为**单一 `http` 服务**：`/api/*` 走 JSON 接口，`/` 及其他走静态文件（含目录穿越防护）。

---

## 4. 数据模型

```jsonc
users:     { id, username, password(哈希), email, bio, role, createdAt }
works:     { id, title, authorId, authorName, category, type, rating, summary,
             tags[], status('ongoing'|'completed'),
             chaptersCount, wordsCount, views, favoritesCount, commentsCount, kudosCount,
             createdAt, updatedAt }
chapters:  { id, workId, title, order, content, wordsCount, createdAt, updatedAt }
comments:  { id, workId, userId, userName, content, createdAt }
favorites: { userId, workId, createdAt }
categories: 静态配置 —— foxiu 佛休(主分类,isMain) / novel 小说 / short 短篇 / setting 设定
type:      作品类型 novel|short|setting（由原 5 分类精简：诗歌散文并入 short，剧本脚本取消）
rating:    内容分级 G|T|M|E，默认仅暴露 G/T，M/E 需年龄确认(mature=1)
kudos:        { userId, workId, createdAt }
notifications:{ id, userId, type, payload, read, createdAt }
follows:      { userId(粉丝), authorId(被关注者), createdAt }
// 注：authorBio 不在 works 表中，由 publicWork() 读 users.bio 动态拼接到接口响应
sessions:  { token: userId }   // 登录态

// 注意：原 5 分类（同人小说/短篇随笔/诗歌散文/剧本脚本/设定资料）已迁移为
// 「主分类 category=foxiu + 类型 type」两字段，旧数据由 migrate-v2.js 转换。
```

字数统计：按中文字符（去空白）计数，章节与作品字数随增删自动重算。

---

## 5. API 一览

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/register` | 注册（密码 ≥6 位，用户名唯一） |
| POST | `/api/login` | 登录，返回 token |
| POST | `/api/logout` | 登出 |
| GET | `/api/me` | 当前用户 |
| GET | `/api/categories` | 分类列表 |
| GET | `/api/tags` | 标签云（含计数） |
| GET | `/api/works` | 列表：支持 `q/category/type/rating/tag/status/sort/page/pageSize/mature` |
| POST | `/api/works` | 新建作品（需登录） |
| GET/PUT/DELETE | `/api/works/:id` | 详情 / 修改 / 删除（仅作者） |
| POST | `/api/works/:id/chapters` | 新增章节（仅作者） |
| GET/PUT/DELETE | `/api/chapters/:id` | 章节内容 / 修改 / 删除（仅作者） |
| GET/POST | `/api/works/:id/comments` | 评论列表 / 发表 |
| POST | `/api/favorites/:id` | 收藏切换（toggle） |
| GET | `/api/me/works` | 我的作品 |
| GET | `/api/favorites` | 我的收藏 |
| POST | `/api/works/:id/kudos` | 🌟 Kudos 点赞切换（toggle） |
| GET | `/api/authors/:id` | 作者主页（作品聚合 + 统计） |
| POST | `/api/authors/:id` | 关注 / 取消关注作者（toggle） |
| GET | `/api/notifications` | 通知列表（含未读数） |
| POST | `/api/notifications/read-all` | 通知全部标为已读 |

---

## 6. 响应式设计策略

- **导航**：PC 横向菜单；移动端（≤680px）折叠为汉堡菜单；搜索框在 ≤680px 保持可见（后续已修复，不再让位给图标）。
- **网格**：作品卡片用 `repeat(auto-fill, minmax(240px,1fr))`，窄屏自动降为 150px 列宽。
- **书库布局**：PC 左侧筛选栏 + 右侧结果；移动端单列堆叠，筛选栏置顶。
- **阅读视图**：居中 760px 栏宽、行高 2.0，字号可 ±调节，护眼/暗色主题一键切换。
- **表格**：后台数据表在移动端转为卡片式（`<td data-label>` + flex）。
- **主题**：通过 CSS 变量 + `data-theme`，用户偏好存 `localStorage`。

---

## 7. 安全要点

- 密码使用 `crypto.scrypt` + 随机盐哈希，明文不落盘。
- 会话用随机 token，`Authorization: Bearer` 校验；服务端对**所有写操作**二次校验「是否作品作者」，前端隐藏按钮只是体验，越权请求会被 403 拒绝。
- 所有用户输入在渲染时经 `escapeHtml` 转义，杜绝存储型 XSS。
- 静态服务做了路径规范化与根目录前缀校验，防止目录穿越。

---

## 8. 运行与部署

```bash
cd foersi-hexiu-fanverse
node server.js            # 默认 3000；可用 PORT=3001 node server.js 指定端口
# 浏览器打开 http://localhost:3000
```

演示账号：`休之笔` / `demo1234`（已内置示例作品与章节）。

数据持久化于 `data/db.json`；删除该文件即重置为初始种子数据。

部署：可整体拷贝至任意支持 Node 的环境；如需公网，可置于 Nginx 反向代理之后，或迁移到 EdgeOne Pages / 云函数（将 `store` 替换为云数据库即可）。

---

## 9. 后续可扩展点（建议）

- **阅读进度 / 续读**：记录每用户每作品最后阅读章节。
- **作品封面图上传**：当前为按 ID 生成的渐变占位，可接入对象存储。
- **审核与举报**：公开平台所需的合规能力。
- **作者主页 / 关注**：作者作品聚合与读者关注流。✅ 已实现（`author.html` + 关注通知）
- **全文检索增强**：接入 Elasticsearch / SQLite FTS。
- **真实数据库**：将 `store.js` 替换为 SQLite/Postgres，提升并发与可靠性。
- **RSS / 更新订阅**：让读者订阅喜欢的作者或标签。
